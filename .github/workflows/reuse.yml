# SPDX-FileCopyrightText: 2026 Thomas Butyn
# SPDX-License-Identifier: MIT
name: REUSE compliance

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  reuse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Verify REUSE 3.3 compliance
        uses: fsfe/reuse-action@v6
